# Copy to .env and fill in. Never commit .env.

# Where the SQLite file lives, relative to prisma/schema.prisma.
DATABASE_URL="file:./dev.db"

# Optional. Without it the app works fully; tasks just show no photo and say why.
# Free key: https://www.pexels.com/api/
PEXELS_API_KEY=
